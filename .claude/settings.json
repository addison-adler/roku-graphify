{
  "skills": {
    "brs-ref": {
      "label": "BrightScript Reference",
      "description": "Look up a BrightScript component, interface, or SceneGraph node from the Roku SDK graph",
      "command": ".claude/skills/brs-ref/brs-ref.sh",
      "takes_args": true,
      "args_help": "component name (e.g., roArray, ifArray, Video)"
    },
    "analyze-app": {
      "label": "Analyze Roku App",
      "description": "Parse a Roku channel's BrightScript + SceneGraph code and generate a Graphify wiki and interactive HTML studio",
      "command": ".claude/skills/analyze-app/analyze-app.sh",
      "takes_args": true,
      "args_help": "<app-dir> [output-dir]"
    }
  },
  "mcpServers": {
    "code-review-graph": {
      "command": "uvx",
      "args": [
        "--from",
        "git+https://github.com/addison-adler/code-review-graph",
        "code-review-graph",
        "serve"
      ]
    }
  }
}
